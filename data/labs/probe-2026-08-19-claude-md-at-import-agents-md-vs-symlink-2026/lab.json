{
  "id": "probe-2026-08-19-claude-md-at-import-agents-md-vs-symlink-2026",
  "pillar": "agent-harness",
  "question": "AGENTS.md 를 Claude Code 에 물리는 공식 두 경로(CLAUDE.md 첫 줄 @AGENTS.md import, CLAUDE.md→AGENTS.md 심볼릭 링크)와 단순 복사가 같은 문서를 같은 값으로 모델에 넣는가, 배선이 없으면 정말 안 들어가는가, 그리고 @import 가 조용히 풀리지 않는 자리는 어디인가.",
  "expectation": "결론부터 적는다. 나는 세 배선(@import·심볼릭 링크·복사)이 전부 6/6 수준으로 도달하고 토큰 총량도 서로 100 토큰 안쪽에서 같을 것으로 본다. 즉 공식 문서가 나란히 적어 둔 두 경로 사이에 실측 차이가 없을 것이라고 본다. 이 예상이 맞으면 이건 확인이고 기사로 밀 값은 약하다. 내가 노리는 것은 나머지 두 셀이다.\n\n셀별 예상. (1) bare-agents 는 0/3 이다. claude 2.1.233 은 CLAUDE.md 계열 파일명만 걷고 AGENTS.md 라는 이름은 로더의 대상이 아니다. 공식 메모리 문서가 지금도 @import 나 심볼릭 링크로 끌어오라고만 안내하는 이유가 이것이다. (2) at-import 는 ZZATIM47·ZZWRAP99 둘 다 3/3 이다. (3) symlink 는 3/3 이다. 열리는 것은 링크가 아니라 타깃 바이트이고 macOS 에서 심볼릭 링크 생성에 권한 문제가 없다. (4) copy 는 3/3 이다. 이 셀이 문서 한 벌 분량의 토큰 기준선이다. (5) 경계는 fenced-import 다. CLAUDE.md 안의 @AGENTS.md 를 마크다운 코드펜스로 감쌌다. 공식 문서는 코드 스팬·코드 블록 안의 import 는 평가하지 않는다고 적는다. 그것이 사실이면 ZZWRAP99 는 3/3, ZZFENC85 는 0/3, total_input 은 bare-agents 수준이다. (6) notes-control 은 0/3 이다.\n\n예상 토큰 표. 기준선을 B 라 하면 notes-control 과 bare-agents 가 B, copy 와 symlink 가 B+약 2,400, at-import 가 B+약 2,400+수십(래퍼 몫), fenced-import 가 B+수십이다. 캐너리 문서는 여섯 셀 모두 같은 생성기로 만든 9,674 바이트다.\n\n이 실험이 앞 랩보다 나아진 지점. 2026-08-18 랩은 '캐너리가 답변에 나타났는가' 하나만 쟀고, miss 가 '로드 안 됨'인지 '로드됐는데 모델이 무시함'인지 구분하지 못한다고 스스로 한계를 적었다. 이번에는 --output-format json 의 usage 합계를 같이 읽어 그 둘을 가른다. 문서가 프롬프트에 들어갔으면 문서가 없을 때보다 2,400 토큰 가까이 더 청구된다. 답변에 캐너리가 없는데 토큰만 올라가 있으면 그건 로드된 것이다. fenced-import 셀은 정확히 이 구분을 위해 설계했다.\n\n무엇이 나오면 기사인가. (a) bare-agents 가 hit 이면 claude 2.1.233 이 AGENTS.md 를 스스로 읽는다는 뜻이고 공식 안내가 낡았다는 뜻이다. 이 경우 오늘 쓸 글의 주장이 통째로 뒤집힌다. (b) fenced-import 의 total_input 이 copy 수준이면 '코드 블록 안의 import 는 평가하지 않는다'는 문장은 렌더링 얘기지 로딩 얘기가 아니라는 뜻이다. CLAUDE.md 에 예시랍시고 코드펜스로 감싸 둔 경로가 조용히 컨텍스트로 들어오고 있다는 실무 함정이 된다. (c) at-import 의 total_input 이 copy 보다 문서 한 벌만큼 더 크면 import 가 이중 계상된다는 뜻이고, 그러면 심볼릭 링크를 고르라는 실무 권고가 처음으로 숫자를 갖는다. (d) symlink 가 miss 면 공식 안내 두 줄 중 하나가 macOS 에서 안 먹는다는 뜻이다. (e) 전부 예상대로면 두 경로가 실측으로 동등하다는 표 하나가 남고, 그 표 자체는 아직 아무도 안 낸 숫자다.\n\n판독 규칙을 미리 정해 둔다. 캐너리 문서는 9,674 바이트로 2026-08-16 랩이 확률적 누락을 관측한 31 KiB 대역에서 한참 멀고 캐너리는 문서 첫머리에 있다. 그러므로 각 셀은 0/3 아니면 3/3 에 붙어야 정상이고, 1/3 이나 2/3 이 나오면 그것은 '로드 안 됨'이 아니라 '작은 문서인데도 확률적으로 못 꺼냈다'는 별개의 발견으로 따로 적는다. 그 경우 total_input 이 판정을 대신한다.\n\n정직한 한계. 반복이 셀당 3 회라 드문 확률적 누락은 잡지 못한다. codex 축은 아예 재지 않는다 — 2026-08-18 랩에서 이 계정의 codex 60 런이 전부 usage limit 으로 죽었고 안내된 해제 시각이 2026-09-15 라 오늘 다시 돌려도 같은 결과가 나온다. Windows 의 심볼릭 링크 권한 문제는 실행 환경이 macOS 라 미측정이다. 그리고 이 결과는 claude 2.1.233 한 버전의 동작이지 AGENTS.md 규격의 동작이 아니다.",
  "environment": {
    "os": "darwin 25.5.0 (macOS 26.5.2, build 25F84)",
    "node": "v22.22.0",
    "tools": {
      "claude": "2.1.233 (Claude Code)",
      "git": "2.50.1 (Apple Git-155)",
      "jq": "jq-1.7.1-apple (/usr/bin/jq)",
      "codex": "0.147.0 (codex-cli, 이번 랩에서는 호출하지 않는다)"
    },
    "models": {
      "claude": "sonnet"
    },
    "notes": [
      "~/.claude/CLAUDE.md 는 10951 바이트이고 여섯 셀 모든 run 에 항상 로드된다. ZZ 로 시작하는 토큰도 canary 라는 단어도 들어 있지 않다(grep 확인). 셀 간 상수이므로 total_input 비교의 기준선에 포함된다.",
      "~/.claude/AGENTS.md 는 존재하지 않는다. 전역 AGENTS.md 가 결과에 섞일 여지가 없다.",
      "캐너리 문서는 여섯 셀 모두 같은 생성기(헤더 + seq 1..60 패딩 루프)로 만들어 9,674 바이트로 동일하다. 캐너리 토큰만 셀마다 다르고 길이는 8 자로 같다. 대략 2,400 토큰 안팎이다.",
      "명령에 --tools \"\" 를 붙여 도구를 전부 없앴다. 모델이 스스로 파일을 열 수 없으므로 hit 은 '세션 시작 시 프롬프트에 들어간 문서'에서만 나올 수 있다. 앞 랩처럼 '파일을 읽지 마라'는 부탁에 기대지 않는다.",
      "--strict-mcp-config 를 --mcp-config 없이 붙여 MCP 서버를 전부 끈다. 이 머신의 사용자 설정에는 도구 수십 개짜리 MCP 서버가 붙어 있고 연결 성공 여부가 run 마다 흔들리면 total_input 비교가 무의미해진다. --disable-slash-commands 도 같은 이유(스킬 목록 주입 제거)다. 세 플래그 조합은 claude --help 로 파싱만 미리 확인했고 모델 호출은 하지 않았다.",
      "~/.claude/settings.json 에 hooks 가 설정돼 있다. 훅이 주입하는 텍스트는 여섯 셀에 동일하게 들어가므로 상수다.",
      "셀마다 CELL_DIR/repo 를 git init -q -b main 으로 만든다. 두 CLI 가 볼 레포 루트가 실제로 있어야 이 실험이 성립한다.",
      "CELL_DIR 은 $TMPDIR(/var/folders/...) 아래라 $HOME 밖이다. repo 위쪽 조상 경로에는 CLAUDE.md 도 AGENTS.md 도 없다. 2026-08-18 랩에서 claude 가 조상 사슬을 git 루트 너머까지 걷는다는 것이 확인됐으므로 이 조건이 중요하다.",
      "runner 는 반복마다 CELL_DIR 을 비우고 setup 을 다시 돌린다. setup 은 3 회 재실행을 전제로 썼고 ln -s 도 매번 빈 디렉터리에서 시작한다.",
      "notes-control 은 파일명이 NOTES.md 여서 어느 로더의 대상도 아니다 — 통제 셀이다.",
      "블로그 레포를 가리키는 명령은 하나도 없다. 모든 작업은 CELL_DIR 안에서만 일어난다.",
      "네트워크는 claude 의 모델 호출에만 쓴다."
    ]
  },
  "dimensions": {
    "claude_md": [
      "absent",
      "at-import",
      "symlink",
      "copy",
      "fenced-import"
    ],
    "canary_doc": [
      "AGENTS.md",
      "NOTES.md"
    ]
  },
  "runs": 18,
  "cells": 6,
  "status": "complete",
  "consumed": false,
  "surprised": false,
  "headline": "배선 없는 AGENTS.md 는 0/3 hit 이고 total_input 도 통제 셀과 −2 토큰이라 9,674 바이트 문서가 프롬프트에 아예 들어가지 않는다. @import(17978)·심볼릭 링크(17856)·복사(17862) 세 배선은 전부 3/3 hit 에 문서 한 벌 2,920 토큰만 청구했고, @import 는 복사보다 116 토큰 많을 뿐 문서를 이중 계상하지 않는다. 같은 `@AGENTS.md` 한 줄을 마크다운 코드펜스로 감싸면 ZZFENC85 0/3 에 total_input 15081(배선 없음 수준 +141)로, 캐너리만 안 나오는 게 아니라 로드 자체가 일어나지 않는다."
}
