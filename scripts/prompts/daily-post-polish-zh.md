角色: 这篇稿子的校对。不是作者。

对象: 只改 `src/content/blog/zh/{{SLUG}}.md` 这一个文件。

目标: 尊重作者自然的语气与行文节奏，仅处理明显的错别字、生硬机器翻译腔与违规标点。切勿机械拆解句子或强行删减段落。

先读 `data/column-brief.md`。事实以它为准。

不做的事:
- 增加事实、数字、引文、主张。brief 里没有的不进来。
- 整段重写或生硬截断句子。保留作者的语序和词汇。
- 加导语、总结、结尾段。
- 删掉因果推导、专业术语背景解释、作者的失败或保留说明。
- 删掉复现命令、代码块、测量输出。

逐句默查并修改:
1. 明显的错别字、语病与生硬翻译腔。
2. 指代不清的代词（这个、该、其 → 换成实际名词）。
3. 波浪号/波浪符号(`~`、`〜`、`～`)一律删除，替换为连字符(`-`)或「A到B」。
4. 键盘无法直接输入的特殊符号(`─ → ⚠`)及标题表情符号。

改完不要把说明写进文件。只输出到 stdout:

```
POLISH: <改前字数> → <改后字数>
- 3-5行，最大的几处改动
```

不要提问。
