# jangwook 루프 운영 플레이북 (wbai 모델 이식, 2026-07-18)

> 30분 틱 루프의 절차서. wbai(ops/LOOP.md)의 설계 원칙을 계승하되 이 시스템의 조건에 맞게 변형:
> **① 메인 루프는 판단·위임만** — 실작업(리서치·집필·QA·개선)은 전부 백그라운드 에이전트. 토큰 폭발 방지가 제1 원칙.
> **② 배달은 launchd, 두뇌는 루프** — wbai와 달리 발신(X 3슬롯 리마인드·블로그 발행·클로징)은 launchd가 담당하므로 세션이 죽어도 배달은 끊기지 않는다. 루프는 그 위에서 소재 수집·품질 감시·보충·자기 개선을 돈다.
> **③ 페르소나 원천은 정본** — 모든 콘텐츠 에이전트는 `docs/persona-kim-jangwook.md`(4기둥+DNA)를 먼저 읽는다.

## 틱 절차 (매 30분)

```
1. Bash: node scripts/loop-tick.mjs        → 컴팩트 JSON 상태 출력
2. JSON 플래그에 따라 아래 매트릭스의 에이전트를 스폰 (해당 없으면 즉시 종료)
3. 에이전트 결과는 한 줄 요약만 수신. 상세 산출물은 에이전트가 직접 텔레그램 전송
   (claude-controller/sh/send-telegram.sh)
```

**토큰 규율 (메인 루프에서 금지)**: 글 전문 읽기, 웹 검색, 집필, 에이전트 트랜스크립트 읽기. 에이전트 반환은 한 줄.
**텔레그램 규율**: 라벨-본문 분리 금지(캡션 활용), 사이클당 에이전트별 메시지 상한 2건, 수정 재전송 시 "[정정판]" 명시.

## 트리거 매트릭스

| 트리거 (tick JSON) | 에이전트 | 산출물 | 전달 |
|---|---|---|---|
| ~~`xqa_due`~~ — <strong>폐지 2026-07-27</strong> (운영자 지시: X 운영 중단) | ① X 큐 QA (폐지) | X 관련 플래그(xqa/xgen_missing/scout)·launchd 잡(morning-daily-x, x-reminders → ~/Library/LaunchAgents/disabled/) 전부 철거. 복구는 git 이력 + disabled/ plist 재등록 | — |
| `catchup_due` — 08시/18시 이후 첫 틱 | ② 캐치업 스카우트 | 공식 소스 스캔(Search Central·web.dev·W3C/WAI·Chrome release·Anthropic/OpenAI 공식·MCP 블로그). 각 건에 "웹 개발/EM 관점에서 무엇이 바뀌나" 한 줄 통찰. 유효 건 → topic-backlog 후보 + X tip/news 소재 제안 | 텔레그램 브리프 (최대 5건) |
| `post_qa_due` — 15:23 발행 후 신규 블로그 커밋 감지 | ③ 발행 QA | 신규 글의 게이트 상태 + 페르소나 DNA 대조(솔직히·공식인용·in-media-res 도입·CTA) + 4언어 스팟체크 + 직접 인용 축자 대조(07-23 교훈) + <strong>상대날짜("N일 전/前/ago") 실날짜 대조 — 가급적 절대 날짜로 교체(07-24 ⑤: 4언어 배달 사고)</strong> + <strong>따옴표 없는 의역 사실주장("공식이 못박는다" 류)도 근거 URL 대조(07-24 ⑤: QA 사각)</strong>. 결함은 직접 수정 커밋 | 판정 요약 텔레그램 |
| `queue_low` — 익일 X 소재 부족 (백로그 queued < 5) | ④ 백로그 보충 | 캐치업 산출물 + 최근 서비스 운영 데이터에서 토픽 3건 브리핑 수준으로 적재 | 적재 요약 |
| `review_due` — 23:30 이후 첫 틱 | ⑤ 자기 리뷰 | 최근 24h 산출물(발행 글·X 3본·클로징 커밋) 비평: 사실 오류, 페르소나 이탈, 반복 패턴, 4기둥 편중(①만 나오고 ②④가 실종 등). 교훈 → 이 파일 "운영 교훈" + 메모리 | 개선점 3줄 |
| `improve_due` — 수요일 첫 틱 (주 1) | ⑥ 웹 개선 | GEO/AIO/SEO/a11y 축 중 하나를 골라 사이트 실측 감사 1건 → 개선 커밋 → 글감이 되면 백로그 적재 (7월 캠페인 플레이북: 측정→수정→게이트) | 감사 요약 |
| `service_pulse_due` — 월요일 첫 틱 (주 1) | ⑦ 서비스 펄스 | 운영 서비스(jangwook.net·effloow·InsightForge·심해식당)의 헬스·지표 훑기. 이상 발견 시만 상세, 콘텐츠 소재(운영 실측 데이터) 후보 채집 | 한 줄/서비스 |
| ~~`scout_due:*`~~ — <strong>폐지 2026-07-27</strong> (운영자 지시: X 운영 중단) | ⑧ X 상호작용 스카우트 (폐지) | — | — |
| `style_campaign_due` — ops/style-campaign.json pending>0 (매 틱) | ⑨ 문체·전개 캠페인 — <strong>완주 2026-07-19</strong> (260/260, 배치 26회, 최종 감사: ko 311편 전부 해라체 우세·구어 파편 0·풀 빌드 그린) | pending 상위 batch_size(10)편을 해라체 전환 + 맺음말 보강 (아래 ⑨ 프롬프트). 에이전트 1명이 순차 처리·매니페스트 갱신·커밋까지 (병렬 금지 — 매니페스트 레이스) | `N편 완료\|잔량 M` |
| `repo_changed` — HEAD 변화 감지 | (에이전트 불필요) | 병행 자동화 커밋 확인용 정보 플래그. 게이트 이상 시에만 ③ 스폰 | — |

## 에이전트 프롬프트 공통 헤더

```
작업 디렉토리 ~/Documents/workspace/www.jangwook.net (X 관련은 ~/Documents/workspace/web.effloow.com).
먼저 docs/persona-kim-jangwook.md를 읽고 4기둥·DNA·금지 목록을 장착할 것.
텔레그램: ~/Documents/workspace/claude-controller/sh/send-telegram.sh "텍스트".
빌드 검증 시 npm run validate:publishing && npm run build (게이트 10종 통과 필수).
완료 시 반드시 SendMessage 도구로 team-lead에게 한 줄 요약을 보고할 것 (최종 텍스트 응답만으로는 메인 루프에 전달되지 않음 — 2026-07-19 실측).
```

## 유형별 프롬프트 요점

**① X 큐 QA**: 폐지 (2026-07-27 운영자 지시 — X 운영 중단)

**② 캐치업 스카우트**: "WebSearch/WebFetch로 [공식 소스 목록]의 최근 48h 발표를 수집. 각 건을 페르소나 4기둥 중 어디에 착지하는지 분류하고 '웹 개발자/EM에게 바뀌는 것' 한 줄 통찰을 붙여라. 뉴스 요약 봇 금지 — 통찰 없는 건은 버린다. 백로그 후보는 data/topic-backlog.json 형식(source에 브리핑 수준 상세)으로 제안만(적재는 ④). 텔레그램 브리프 전송."

**⑤ 자기 리뷰**: "최근 24h: 블로그 신규 글(git log), X 3본(sent/), 클로징 커밋을 읽고 비평 — 사실 오류·페르소나 DNA 이탈(2절 수치 감각으로)·4기둥 편중·AI티 재발. wbai 교훈 준용: 재생성 시 직전 리뷰 정정의 회귀 여부를 반드시 확인. 시스템 교훈은 ops/LOOP.md '운영 교훈' 최상단에 추가. 개선점 3줄 텔레그램."

**⑧ X 상호작용 스카우트**: 폐지 (2026-07-27 운영자 지시 — X 운영 중단)

**⑨ 문체·전개 캠페인 (2026-07-18 개시, AdSense 재도전 결정)**: "ops/style-campaign.json에서 status=pending 상위 10편을 읽고, 각 ko 글을 순차 수정 — (a) 본문 산문을 해라체(했다/이다/한다 단정문)로 전환. 보존: frontmatter 전체(제목·description·FAQ 불변)·코드 블록·인용문 안의 대사·표 안의 짧은 구. 구어 파편(~죠/~거든요/~네요/~인가요 종결)과 이모지는 제거 (b) needs에 closing이 있으면: 실질적 마무리 섹션(정리/체크리스트/다음 단계 성격)이 이미 있는지 먼저 판단하고, 정말 없을 때만 마지막에 '## 정리' H2(해라체 3〜6문장: 핵심 요약+실행 포인트 — 계몽조 금지, 페르소나 5절 준수)를 추가. 이미 있으면 needs 오탐이므로 그냥 통과 (c) in-media-res 도입은 절대 훼손 금지 — '이 글에서는' 류로 바꾸지 말 것. 각 편 완료 시 매니페스트 status를 done으로 갱신. 10편 후 npm run validate:publishing 통과 확인 → 매니페스트 포함 일괄 커밋 `style(blog): 문체 캠페인 배치 — 해라체 N편·맺음말 M편` → push. 반환: `N편 완료|잔량 M`"

## 상태 관리

- `ops/loop_state.json` (gitignore): last_xqa / last_catchup(am|pm) / last_post_qa / last_review / last_improve / last_service_pulse / last_head — 틱이 due 보고와 동시에 마킹.
- 루프 등록: `/loop 30m` (세션 내 유효). 세션 재시작 시 이 파일 기준으로 재등록.
- launchd 의존성: daily-post(15:23)·daily-closing(23:07)·sunday-strategy — 루프가 죽어도 이들은 돈다. 루프는 품질·소재·개선 레이어만 담당. (X 계열 morning-daily-x·x-reminders는 2026-07-27 폐지 — plist는 ~/Library/LaunchAgents/disabled/)

## 메모리 라우팅 (wbai 규칙의 이 시스템 판)

| 정보 유형 | 저장소 |
|---|---|
| 페르소나·보이스 (헌장) | `docs/persona-kim-jangwook.md` — 분기 재측정 외 수정 최소화 |
| 운영 교훈 (프로세스) | 이 파일 "운영 교훈" 섹션 (append-only, 최신 위) |
| 토픽·소재 (정형) | `data/topic-backlog.json` (source 필드 = 브리핑) |
| 품질 불변식 | `scripts/validate-*.mjs` (게이트 코드가 곧 문서) |
| 세션 간 컨텍스트 | Claude 메모리 (`project_*` 파일) |
| X 산출물 | web.effloow.com `contents/<ymd>/x/daily/` (게시 후 정리는 effloow 쪽 관행) |

## 자기 개선 회로

⑤ 리뷰의 교훈 → 이 파일 운영 교훈 → 다음 틱·에이전트가 로드 → 산출물 반영. 페르소나 차원의 이탈이 반복되면 정본을 고치지 말고 파이프라인 프롬프트를 고친다 (정본은 코퍼스 재측정으로만 갱신 — wbai의 "헌법 불변" 원칙 준용).

## 운영 교훈 (append-only, 최신이 위)

- 2026-07-27 (운영자 지시): **X 운영 전면 폐지**. "X를 운영해나가는 메리트를 못 느낀다" — 루프의 X 플래그(xqa_due·xgen_missing·scout_due) 제거, launchd 잡 2개(morning-daily-x 07:00, x-reminders 3슬롯) bootout 후 plist를 ~/Library/LaunchAgents/disabled/로 이동. ②캐치업의 "X tip/news 소재 제안"과 ⑤리뷰의 "X 3본" 항목도 이후 스폰 프롬프트에서 제외할 것. 복구 시: git 이력(이 커밋) + disabled/ plist 재부트스트랩. web.effloow.com의 스킬·voice-guide·산출물은 보존(삭제 아님).

- 2026-07-26 (자기 리뷰 ⑤, 폴백 체제일): **두 개의 맺음·도입 정형화 템플릿이 정량으로 굳었다 — 하나는 sunday-strategy가 이미 관찰(도입 "기대치 깎기"), 다른 하나는 미포착(상담 CTA 맺음말)**. 최근 ko 15편 grep 실측: (a) **"기대치를 깎" 메타 문장 5편**(webmcp「여기서 미리 기대치를 깎아둔다」/ websocket-bfcache「여기서 기대치를 미리 깎아둔다」/ bfcache-audit「먼저 기대치를 깎아두자」/ restaurant-jsonld「기대치를 깎고 가자」/ robots-snippet「여기서 기대치를 깎아야 한다」) — 페르소나 가치 #2(기대치 깎기)는 옳으나, 이걸 <strong>"여기서 기대치를 깎아둔다"라고 예고하는 문장</strong>은 k16 규범의 "본문 자기 언급/문서 갱신 문장"(삭제 대상)이다. 한계를 그냥 진술하면(「오리진 트라이얼은 정식 표준이 아니다」) 될 것을, "지금부터 기대치를 깎겠다"는 메타 프레임으로 감싸는 것이 반복되면 그 자체가 AI티. (b) **상담·구현 의뢰 CTA 7편**(webmcp·websocket-bfcache·bfcache-audit·modal-focus·robots-snippet·content-visibility·wcag22-target — 거의 절반) — 「[구체작업A]이나 [구체작업B]하고 싶다면, 상담·구현 의뢰를 받는다. 프로필 문의로 연락」 동일 구조. ④서비스빌더 기둥상 의뢰 자체는 정합이나, 절반 편에서 <strong>페르소나 시그니처 맺음(체크리스트·판단기준·루프 사고 회귀)을 밀어내고 세일즈 클로즈로 대체</strong>되는 게 문제다. 07-24·07-25 ⑤가 이 CTA를 "주간 쿼터 내 허용/접점"으로 통과시켰으나, 15편 중 7편 동일 템플릿이면 쿼터가 아니라 상수다. ③ QA는 사실 오류가 아니라 통과시킨다(정형화는 QA 사각). <strong>처방</strong>: (1) daily-tech-blog SKILL에 "기대치 깎기는 한계를 직접 진술로만 — '기대치를 깎아둔다/깎고 가자' 류 예고 메타 문장 금지"를 명문화. (2) 상담 CTA는 매 편 기본값이 아니라 선택 — 최소 절반 이상은 페르소나 시그니처 맺음(체크리스트/1인칭 판단/열린 재측정 과제)으로 끝내고, CTA를 넣더라도 템플릿 문형을 매번 바꿀 것. **스카우트 신규 관찰**: 리플/인용 초안이 <strong>검증 불가능한 1인칭 실측을 구조적으로 생성</strong>한다(morning#1「モバイル実機の操作ログで測ったことがあります…INPも悪化」, morning#3「うちのGAでもオーバーレイ中エンゲージメントが落ちてました」, evening#3「設計ドキュメントを分けたことがある…二重管理の工数」). 스카우트 에이전트는 Kim의 실 GA·측정 이력에 접근할 수 없으므로 이 실측들은 그럴듯하게 발명된 것일 개연이 높다 — 페르소나 5절 "실측 없는 체험담 날조=최악의 위반"의 상시 노출면이고, ed09673(직접 인용 날조)과 같은 계열. 최종 게이트가 Kim의 수동 검토라는 점이 방어막이나, "내가 했을 법한" 실측이라 빠른 검토를 통과할 위험이 실질이다. <strong>처방</strong>: ⑧ 프롬프트의 "자기 실측 한 스푼(없으면 억지 각도 제외)"을 강화 — 구체 수치·특정 측정("測ったことがある/GAで落ちてました") 대신 <strong>일반 원리·판단 기준·질문</strong>으로 각도를 잡고, 검증 가능한 실측은 블로그 링크로만 귀속. **폴백 체제 품질 평가(team lead #4)**: 폴백 산출물 자체는 통상일과 동등 — 블로그 4언어 transcreation 견고(ja/en/zh 모두 번역투 없는 독립 집필, ③ QA 인용 5건 축자 무결), X post-2 앵커는 실측 서사(violations:0の緑=床であって天井ではない)·겸손 맺음(「自分もやってたので人のこと言えない」)으로 우수. 위 정형화 패턴은 15편(통상일 포함)에 걸쳐 있어 <strong>폴백 특유 저하가 아니라 파이프라인 프롬프트의 시스템 드리프트</strong>. **회귀 확인**: (1) 슬롯 tip 고착=<strong>해소</strong>(07-25 처방대로 post-1=news, tip 2일 연속 회피 이행). (2) 날짜 드리프트=<strong>없음</strong>(post-1「先月」=2026-06 정확, blog pubDate 정확 — 3회 재발군 2일째 청정). (3) 이모지=X 전건 0이나 <strong>오늘 폴백 생성이라 생성단계 미검증</strong> — 실검증은 내일 07:00 정규 생성분 ① QA 수정 0건 여부(4881554/morning-daily-x 상설화의 첫 정규 시험). (4) 헤지 어미=스카우트 레벨은 분산 관리 성공(morning ①なんですよね②평서③질문형, evening 6유형 2/일 상한 자체 계산)이나 <strong>블로그 도입 메타문장 수렴("기대치 깎")은 미해소</strong> — 어미는 잡혔는데 문장 골격이 굳었다. **4기둥**: 오늘 건강 — 블로그 ③(AI캐치업)→①(웹개발/보안) 착지, X ③(post-1 faqpage)/①(post-2 a11y)/④(post-3 light) 3기둥, ②는 부재이나 주간 쿼터 충족(07-24). 편중 없음.

- 2026-07-26 (틱 운영): **claude 헤드리스(-p) 시스템 전반 행업 — 하루 5건, 폴백 에이전트 체제로 방어**. 07:00 X 생성 ×2, topic-intelligence(39분), page-quality-repair(59분), lead-readiness-check(30분), 15:23 daily-post까지 모든 `claude -p` 호출이 0% CPU(누적 0:00.00) 무한 대기 — `claude -p "reply OK"` 60초 새니티 테스트로 CLI 헤드리스 자체 고장 확정(세션 내 서브에이전트는 정상). 판별법: `ps -o time -p <claude pid>`의 누적 CPU가 10분+ 경과에도 0:00이면 행업. 대응: X 3본·daily-post 모두 폴백 에이전트 직접 수행으로 배달 유지, 행업·고아 프로세스 kill 정리. 원인은 CLI 버전/인증 계열 추정 — 재발 시 ⑥에서 (a) 각 launchd 잡에 새니티 프리플라이트(60초 OK 테스트, 실패 시 즉시 텔레그램 알림) (b) effloow-scheduler 스텝별 타임아웃 도입을 다룰 것.

- 2026-07-25 (자기 리뷰 ⑤): **다수 이전 ⑤ 처방이 동시 해소된 첫 사이클 — 그리고 미문서화 사각 1건(슬롯 유형 반복의 "질감 교체" 예외)**. 회귀 확인 5건: **(1) 발행 글 사실성 = 청정**. faqpage-deprecation-ai-citation-2026의 타임라인 4개 날짜(2023-08 정부·보건 한정+HowTo 폐지 / 2026-05-07 표시 완전 중단·문서 deprecation 명기 / 2026-06 SC 리포트·RRT·필터 제거 / 2026-08 SC API 종료)를 웹 대조 <strong>전건 일치</strong>, ③ QA의 교정(e248424, 날조된 2026-01 마일스톤 제거)이 정확했음을 재확인. 07-24 처방("귀속된 의역 사실주장도 근거 URL 대조")의 첫 실적용 — 「SearchEngineLand 54%→17%」 통계를 검증하니 Portent 분석·SEL 2023-10 게재로 실재(53.94%→17.04%)이고 글이 "공식 아님·참고값"으로 정확히 헤지. <strong>QA가 놓친 사실 오류 0건</strong>. **(2) 날짜 드리프트 = 해소**. 07-24 "사흘 전"(4언어 배달사고)과 달리 오늘 발행글은 절대날짜로 일관, X 본편 상대날짜도 정확(post-2「昨日」=bfcache 재측정 07-24 발행일과 일치). 3회째 재발군이 이번엔 끊김. **(3) 헤지 어미 수렴 = 해소**. 07-24 본편 「〜んですよね」 2회(상한)였으나 오늘 본편 3본 모두 <strong>0회</strong>, 종결도 분산(…なりました/笑いました/질문형). 스카우트도 아침+저녁 합산 「気がします」 2회 상한을 스스로 계산·관리. **(4) 스카우트 3슬롯 파일화 = 완전 이행**. 07-24 부분 이행(morning만 저장)이 오늘 morning·noon·evening 3파일 모두 저장으로 완결(07-23 "파일도 남기게" 처방 종료). **(5) 이모지 = 오늘 발행분 청정(① QA 제거)**, 단 생성단계 3일 연속 재발이라 오늘도 ① QA 수정 1건이 필요했음 — 생성 프롬프트 금지 상설화(4881554)는 오늘 <strong>07:11</strong> 커밋으로 오늘 07:03〜07:10 생성분보다 늦다. 진짜 검증은 07-26 07:00 생성분에서 ① QA 수정 0건 여부(일요일도 morning-daily-x는 07:00 실행). <strong>신규 사각 1건</strong>: 아침 슬롯 tip 2일 연속(260724·260725, 3연속은 아님). post-1 주석이 "news 대체 조건(당일 공식 발표) 불충족 → 유형 유지, 대신 질감 교체(어제=인프라 감사 체험→오늘=보안 사건 소감)"로 정당화했다. 07-20 지시 "전날 같은 슬롯 2연속 금지"에 <strong>문서화되지 않은 예외("질감만 바꾸면 유형 반복 허용")</strong>가 실무에서 생겼다. 1회는 합리적 적응이나 매일 "질감 교체"를 사유로 tip이 고착되면 규칙이 형해화된다. <strong>처방</strong>: 유형 반복은 최대 2일까지만 허용하고 3일째는 강제 교체하도록 생성 프롬프트에 명문화(질감 교체는 2연속의 완화지 무제한 면제가 아님). 페르소나 정합: 발행 글 우수 — in-media-res 도입(안 나오는 아코디언 장면)·공식 인용(2023-08 blog·Google 공식 안내)·기대치 깎기("순위·노출 보장 안 함, FAQPage가 산증인")·1인칭 판단("나라면 신규엔 안 넣는다")·한계 명시(AI 인용 우위는 실무 판단이지 공식 수치 아님 명기)·체크리스트 마감. ko/ja 제목 모두 대시 대신 마침표(k16·house 정합).

- 2026-07-25 (틱 운영): **😇 3일 연속 재발 → 생성 단계 상설화로 종결**. ① QA가 3일째 같은 이모지를 제거(게이트는 작동)했으나 원인은 생성 프롬프트 — web.effloow.com/engine/morning-daily-x.sh의 양쪽 프롬프트 분기에 「絵文字は全投稿で使用禁止(0個)」를 삽입해 상설화(해당 디렉토리는 git 미관리라 이 기록이 유일한 변경 이력). 검증은 내일 07:00 생성분에서 ① QA 수정 0건 여부로 확인.

- 2026-07-24 (자기 리뷰 ⑤): **QA 축자 대조가 "따옴표 안 인용"만 보고 상대 날짜 서술을 놓쳤다 — 발행 글이 4언어 모두 "사흘 전"으로 하루 어긋났다**. websocket-bfcache-eligibility-remeasure(d69a417)가 도입부·description에서 정오표를 단 시점을 「사흘 전」(ja「三日前」)이라 썼는데, 정오표 커밋은 39dea02=<strong>07-22(이틀 전)</strong>다(07-24 기준 사흘 전은 07-21 — 원 감사글 발행일이라 "옛 측정"의 대상은 되지만 "재측정 예정이라 적어둔" 행위=정오표 추기는 07-22). 4언어 전판이 같은 오차라 생성 FACT CORE 단계의 상대 날짜 계산 오류다. ③ 발행 QA는 인용 4건 축자 대조를 통과시켰으나 상대 날짜("사흘 전"·"三日前"·"three days ago")는 대조 대상이 아니었다. <strong>처방</strong>: 발행 QA 체크리스트에 "본문의 상대 날짜 서술(N일 전/前/ago)은 참조 커밋·이벤트의 실제 날짜와 대조" 항목 추가. 날짜 드리프트 재발군(07-20·07-23에 이어 3회째) — 이번엔 산출물 본문에 실린 배달 사고다. **회귀 확인 4건**: (1) 😇 이모지 = <strong>미해소·2일 연속 재발</strong>. post-1(tip)이 어제에 이어 오늘도 「触ってない」ゾーンですね😇로 끝남. 07-23 교훈이 기록만 되고 게이트/프롬프트에 코드화 안 됨 — voice-guide "이모지 0"이 생성단계에서 강제되지 않는다. AdSense 포지셔닝과 상충하는 잔결이 이틀째. 처방: send-daily-x 전 이모지 스캔을 게이트로(정규식 차단). (2) 헤지 어미 수렴(07-23) = <strong>부분 해소</strong>. 아침 리플 스카우트가 교훈을 흡수해 3건 어미를 서로 다르게 분산(面白いところでした/印象です/意外でした)하고 「んですよね」 전면 배제 — 프롬프트 전파 성공. 단 본편 3본은 여전히 「〜んですよね」 2회(post-2 属性なんですよね·post-3 承認してきたんですよね)로 상한 도달, 아직 여유 없음. (3) 슬롯 고착 = 해소. post-1 tip(전날 light)·post-3 light(전날 engage) 전날 대비 유형 교체 확인, 3연속 없음. (4) 4기둥 편중 = <strong>개선</strong>. 오늘 산출이 ①(블로그 bfcache·post-2 모달)·②(post-3 리뷰 프로세스)·③(post-1 gcloud MCP) 3기둥 분산 — 07-18·07-19에 반복 지적된 ②실종이 끊김. ④서비스빌더는 X 본편엔 없으나 블로그 말미 상담 CTA로 접점(주간 쿼터 내 허용). <strong>신규 관찰 2건</strong>: (a) 스카우트 파일 산출 — replies-morning.md는 저장됨(07-23 "파일도 남기게" 처방 이행 확인)이나 follows-noon·replies-evening은 23:38 현재 미저장(정오·저녁 슬롯은 아직 파일화 안 됨, 부분 이행). (b) 블로그가 "Cache-Control: no-store bfcache 롤아웃 2025년 3월〜4월 완료"를 "Chrome 공식 문서가 못박는다"로 단정 — 따옴표 없는 의역 사실주장이라 QA 축자 대조 대상 밖. 날짜·주체 귀속이 검증됐는지 불명, 측정주의 페르소나에선 인용 날조와 같은 노출면. 처방: 출처 귀속된 의역 사실주장도 "축자 아님"이라고 QA를 통과시키지 말고 근거 URL 대조 대상에 포함. 페르소나 정합 자체는 우수(in-media-res 도입·기대치 깎기(순위요소 아님)·6줄 재현 코드·마지막 문단 한계 명시 4중·1인칭 판단) — 발행 글 본체 품질 결함 아님, 문제는 상대 날짜와 의역 사실주장의 QA 사각지대다. ja 제목은 k16 규범대로 대시 대신 「。」 사용(정합), ko 제목만 「—」 잔존(한국어는 규범 미적용이라 house style 허용).

- 2026-07-24 (⑧ 정오 실측): **x-scout.sh는 애초에 동기 블로킹 — 교착의 원인은 스크립트가 아니라 에이전트의 불필요한 Monitor 대기였다**. 정오 스카우트가 실측으로 확인: 스크립트는 grok CLI(`grok -p`)를 동기로 돌려 stdout에 결과를 뱉고 exit 0 하므로 foreground 실행이면 Monitor/폴링 자체가 필요 없다(아래 07-24 "인프라 수정 필요" 항목의 전제 일부 정정 — 수정할 것은 스크립트가 아니라 스폰 프롬프트의 실행 지시). 추가 실측 2건: (a) 스크립트 위치는 web.effloow.com이 아니라 www.jangwook.net/scripts/x-scout.sh, 인자는 슬롯명이 아닌 `replies|follows|likes`(noon은 follows+likes 2회 호출) — ⑧ 템플릿 문구를 이번에 실측 기준으로 갱신함. (b) 출력 앞에 grok 서두 문장이 붙어 첫 `[` 이후만 JSON 파싱해야 함. ⑥ 후보는 "스크립트 동기화"에서 "완료" 처리.

- 2026-07-24 (틱 운영): **스카우트 완료-대기 교착 3회째 — 넛지 대응은 한계, 인프라 수정 필요**. 07-23 정오·07-24 아침 모두 x-scout.sh가 exit 0으로 끝나 있는데 에이전트가 완료 대기로 20분+ 교착(프롬프트에 "대기 전 exit 확인" 지시를 넣어도 초기 실행 시점엔 미완료라 대기 진입 후 이벤트를 놓침). 다음 ⑥ 웹 개선(수요일) 후보로 x-scout.sh를 동기 실행(스크립트가 완료까지 블로킹 후 결과 출력)으로 바꾸거나 완료 마커 파일 폴링 지시로 전환할 것 — 검출·넛지는 매번 사람(루프) 개입 비용이 든다.

- 2026-07-23 (자기 리뷰 ⑤): **날조된 직접 인용이 생성 단계에서 걸러지지 않는다 — 인용 검증 게이트 부재**. 신규 글 modal-focus-escape가 MDN 인용으로 `"become inert (as if the inert attribute is specified)"`를 따옴표로 붙였는데, MDN `<dialog>` 페이지에 <strong>존재하지 않는 문자열</strong>이었다(이번 ⑤에서 원문 대조로 확정 — 실제 문구는 "this behavior is provided by the browser"). QA(e92e400)가 잡아 교정했고 교정본은 원문과 일치함을 재확인했으나, 이건 사후·확률적 포착이다. 페르소나 DNA에서 <strong>공식 문서 인용 78%</strong>가 시그니처인 만큼 인용 날조는 저빈도 엣지가 아니라 상시 노출면이고, 페르소나 5절의 "날조"(최악의 위반)에 인용 날조가 포함된다. 같은 글의 다른 직접 인용(APG "Windows under a modal dialog are inert…", APG Tab 순환 문구)은 이번에 원문 대조로 정확 일치 확인 — 즉 인용을 안 지어내기도 하지만, 지어낼 때 막는 장치가 없다. <strong>처방</strong>: 생성 단계 규칙 — 따옴표 안에 특정 출처(MDN·W3C·Google·스펙)를 귀속시킨 문자열은 반드시 (a) fetch한 원문과 축자 대조하거나 (b) 따옴표를 떼고 의역으로 강등. daily-tech-blog SKILL + ③ 발행 QA 체크리스트에 "직접 인용 부호 안 문자열은 원문 대조" 항목을 박을 것. 회귀 확인 3건: 요일 오류=수복(loop-tick 실측값 주입 7122959, 오늘 헤더 「목」이 하드코딩 아닌 참조로 정확), 슬롯 고착=해소(QA가 post-1 tip→light 전환, slot-1 tip 3연속 차단), bfcache 정오표=존재하나 <strong>재측정 글이 topic-backlog 미적재</strong>(정오표만 얹혀 열린 루프 — 별도 글 착수가 사람 기억에만 의존, 백로그 적재 필요). 어조 개혁 1일차: post-3(첫 존댓말)은 딱딱한 경어가 아니라 구어체 존댓말로 자연 안착(아포리즘 완화·미완결 질문 종결 준수), 단 post-1의 😇 이모지 잔존(AdSense 포지셔닝과 상충)과 post-3 주석의 날짜 오타("2026-07-24"→실제 07-23, 날짜 드리프트 재발군)는 소잔결. 저녁 리플 초안 5건은 SendMessage 요청으로 회수해 대조함(스카우트가 파일 미저장·텔레그램만 하는 구조라 상시 리뷰엔 애드혹 요청 의존 — 파일 산출도 남기게 할 것). 5건 전부 개혁 3규칙 준수: です・ます 종결·아포리즘 닫기 0·「正直」1회·태그/이모지 0·각 건 자기 실측 스푼. <strong>단 신규 관찰</strong>: 미완결/인상 종결이 「〜感じがあります/〜なんですよね/〜気がします」 한 완화-헤지 박자로 수렴(5건 중 4건). 아포리즘 닫기를 제한한 자리가 단일 헤지 어미로 굳으면 그 자체가 새 AI티가 된다 — 완화 종결의 형태 분산을 voice-guide에 추가할 것.

- 2026-07-23 (틱 운영): **Monitor 이벤트 지연 = 무보고 idle의 세 번째 원인**. ⑧ 정오 스카우트가 x-scout.sh 완료(exit 0) 후에도 Monitor 이벤트가 안 와 27분 교착 — 도구 미노출(07-19)·프롬프트 누락(07-22)에 이어 세 번째 패턴. 트랜스크립트 tail로 "Monitor 대기 중" 문구를 확인하면 즉시 판별 가능하고, SendMessage 넛지 한 번으로 풀린다(에이전트는 기존 결과를 그대로 사용 — 재실행 불필요였음). 스카우트류 프롬프트에 "Monitor 대기 전에 스크립트 exit 상태·출력 파일 존재를 먼저 확인, 이미 완료면 Monitor 없이 진행" 지시를 넣을 것.

- 2026-07-22 (자기 리뷰 ⑤): **요일 오류가 산출물이 아니라 오케스트레이션 층에서 재발 — 그리고 앵커 X가 뒤집힌 결론을 실어 이미 배달됨**. 두 건. (1) 07-20 처방 (1)의 "생성일 요일 주입"은 daily-x 생성 프롬프트에는 반영됐으나 ⑤ 리뷰 태스크 텍스트에는 미적용이라, 오늘 리뷰 임무 헤더가 2026-07-22를 「화」로 명기했다(실제 <strong>수요일</strong> — `date` 확인·git 커밋 타임스탬프 "Wed Jul 22"·클로징 백로그의 "내일=목"과 3중 대조로 확정). 발행 산출물(X·블로그·클로징)의 요일 서술은 전부 정상이라 배달 사고는 아니지만, 요일 주입 처방이 "생성"에만 걸리고 "틱/리뷰 스폰 프롬프트"에는 안 걸리는 구멍이 드러났다. 처방: loop-tick.mjs가 요일을 계산해 상태 JSON에 싣고, 스폰 프롬프트는 요일을 하드코딩하지 말고 그 값을 참조. (2) 오후 캐치업이 기존 글 bfcache-notrestoredreasons-audit-2026의 "접속중 WebSocket=차단" 결론이 web.dev 2026-06 발표로 뒤집혔을 가능성을 발굴했는데, 같은 날 12:30 앵커 X(post-2)가 바로 그 기사를 翻案해 "弾かれたのは…接続中のWebSocket"을 공개 게시한 뒤였다. 즉 뒤집힌 결론이 블로그(4언어)와 이미 나간 X 양쪽에 살아 있다. 앵커 X는 신착 블로그를 翻案하는 구조라, 캐치업이 "기존 결론 뒤집힘"을 잡으면 그날 나간 翻案 X와도 대조해야 한다(현 QA는 당일 생성분 내부 정합만 본다). <strong>처리 방침</strong>: 재측정(6프로브 재실행) 전까지 결론을 새로 단정하지 말 것 — 성실성 위반이다. 블로그에는 즉시 최소 정오표(날짜 붙여 "web.dev 2026-06 발표로 이 절의 WebSocket 결론을 재측정 대기 중, 그 전까지 신뢰하지 말 것")만 얹고, 정정된 실측은 재측정 후 별도 글로(측정→수정→게이트). 방치는 측정주의 페르소나의 핵심 가치를 정면으로 깨므로 채택하지 않는다.

- 2026-07-22 (틱 운영, 원인 확정): **launchd exit 78의 정체는 TCC — 로그 경로가 ~/Documents 하위면 잡이 실행조차 안 된다**. StandardOutPath가 ~/Documents(TCC 보호영역) 하위라 파일별 com.apple.macl 라벨이 launchd-spawn 프로세스의 로그 열기를 거부 → EX_CONFIG(78), 스크립트 미실행·로그 미기록. launchd는 exec 전에 로그 fd를 먼저 열기 때문에 스크립트 내용과 무관하게 죽는다. 수복: morning-daily-x·x-reminders plist의 StandardOut/ErrorPath를 ~/Library/Logs/effloow/(TCC 비보호)로 이전, bootout+bootstrap 재적재, kickstart로 exit 0 검증. 규칙: **launchd 잡의 로그·작업 경로는 TCC 보호영역(~/Documents·Desktop·Downloads) 밖에 둘 것**. 07-21 rc=1은 별개 건(claude 헤드리스 개별 실패)으로 잔존 — 내일 07:00 결과로 판정.

- 2026-07-22 (틱 운영): **07:00 X 자동 생성 2일 연속 실패 — 타임아웃이 아니라 환경 문제**. 07-21·07-22 모두 claude 헤드리스가 즉시 rc=1 fast-fail(07-22는 launchd exit 78 config로 로그조차 미생성). 워치독 타임아웃 계열이 아니라 claude CLI 설정/인증 계열로 의심 — 폴백 수동 재생성으로 배달은 지켰으나(07-22: 3본, 07:30 결번·12:30부터 정상) 근본 원인 미해결 상태로는 매일 아침 결번이 반복된다. `xgen_missing` 플래그가 뜨면 재생성과 동시에 원인 진단을 별도 에이전트로 스폰할 것. 진단 포인트: launchd plist 환경(PATH·HOME), claude CLI 인증 토큰 만료, exit 78의 EX_CONFIG 의미.

- 2026-07-22 (틱 운영): **SendMessage 무보고 재발 — 하니스 의심 확정 단계로**. ⑧ 스카우트가 작업·텔레그램 전송(200×7)을 정상 완료하고도 최종 텍스트로만 보고해 메인 루프에는 idle 알림만 도착했다(07-19 교훈의 재발 조건 충족). 같은 틱의 ⑥ 개선 에이전트는 SendMessage 보고에 성공 — 즉 도구 자체는 노출되며, 프롬프트의 "완료 시 반드시 SendMessage" 문구 유무가 갈랐다(⑧ 스폰 프롬프트가 공통 헤더의 해당 줄을 누락). 처방: 스폰 시 공통 헤더를 요약·발췌하지 말고 전문 포함할 것. 무보고 idle을 받으면 트랜스크립트 tail 확인이 재촉 메시지보다 싸고 확실하다.

- 2026-07-20 (자기 리뷰 ⑤): **QA 통과 도장이 수정 전 본문에 찍혀 있다** — 07:00 생성분 post-1·post-12가 2026-07-20(월)을 「日曜」로 서술했다(생성 로그의 구성표에도 "일요일 아침"·"일요일 밤"으로 남아 있다). 07:18에 「月曜」로 고쳐졌으나 X 큐 QA(review.md)는 07:05 산출이라 이 오류를 잡지 못했고, 수정 후 재심사도 없었다. 그 결과 review.md는 <strong>현재 파일에 존재하지 않는 문장</strong>(post-12의 「説が濃厚になってくる」)을 근거로 인용하며 Publish를 판정하고 있다. wbai 이식 교훈 ②(시효성 소재는 지연되면 거짓이 된다)의 첫 실전 위반이자, 검수 산출물의 신선도 문제다. 처방 두 가지: (1) 생성 프롬프트에 <strong>생성일의 요일을 명시 주입</strong>하고 light 유형의 요일·시간대 서술은 그 값과 대조 (2) post-*.md의 mtime이 review.md보다 나중이면 <strong>QA를 무효로 보고 재심사</strong>(틱에서 mtime 비교로 자동 감지 가능).

- 2026-07-20 (자기 리뷰 ⑤): **같은 지표가 한 글 안에서 두 값** — 신규 글 string-lang-dir-metadata-multilingual-web은 감사 절에서 `html[lang] present : 1248/1248`, 게이트 절에서 `html[lang]: 1288/1288 pages`를 제시한다(4개 언어판 전부 동일). 실제 dist는 index.html 1,292 / blog 경로 1,256이다. 즉 파이썬 감사기는 <strong>블로그 페이지만</strong> 셌는데 본문은 "dist/를 훑어"라고 적었고, 게이트(node)는 전 페이지를 센다. 태그·목록 등 40여 페이지가 감사에서 조용히 빠진 셈이고, 독자에게는 설명 없는 수치 점프로 보인다. 1차 출처 검증은 통과했다(W3C string-meta FPWD 2026-07-16, MUST 문장·first-strong 실패 유형 인용 모두 원문 일치). 측정주의를 표방하는 페르소나에서 <strong>수치 불일치는 사실 오류보다 비싸다</strong> — 발행 QA 체크리스트에 "글 안에서 같은 지표가 두 번 등장하면 모수 정의가 같은지 대조" 항목을 넣고, 감사 스크립트와 게이트 스크립트는 반드시 <strong>같은 파일 집합</strong>을 대상으로 하거나 본문에 모수 차이를 명기할 것.

- 2026-07-20 (자기 리뷰 ⑤): **회귀 확인 3건 중 2건 이행, 1건 미이행 — 그리고 3본 축소가 만드는 새 사각지대**. (a) 폴백 재생성 경로의 직전-리뷰 체크리스트: <strong>미이행</strong>. engine/morning-daily-x.sh에는 LOOP.md·직전 리뷰를 참조하는 지시가 없고, 실패 시 조치는 텔레그램 수동 넛지뿐이다. 다만 07-18·07-19 정정이 daily-x SKILL에 코드화되는 우회로가 작동해 자동 경로는 지켜졌다 — 즉 <strong>수동 경로만 아직 사람 기억에 의존</strong>한다. (b) ②EM·④서비스빌더 주간 쿼터: <strong>이행</strong>. SKILL에 명문화됐고 post-4(EM 권한위임)·post-11(개인 서비스 모니터링)로 실제 충족 — 2일 연속 실종이 끊겼다. (c) SendMessage 보고 규율: 이행. 남은 문제는 질(質)이다. ②④가 실린 자리는 둘 다 <strong>질문형 light/engage</strong>여서 "자기 데이터가 실린 ②④"는 여전히 0이고, 블로그는 오늘도 ① 단독이다. 3본 축소의 득실: 득은 본당 밀도와 생성 부하 감소(같은 기사 2각도로 희석하던 post-5/8 패턴이 사라진다), 실은 <strong>주 21칸에 ②④ 쿼터가 얹히면 post-3 슬롯이 사실상 쿼터 전용이 되어 "질문만 던지는 ②④"가 고착</strong>될 위험과, 유형 2연속 판정 기준이 "전날 같은 슬롯"으로 옮겨갔는데 QA는 여전히 당일 3본만 본다는 사각지대다. 주간 배열이 같은 모양으로 굳는지는 아무도 보고 있지 않다 — 일요일 전략 리뷰에 <strong>주간 슬롯 배열표(7일×3슬롯 유형·기둥)</strong> 점검을 추가할 것.

- 2026-07-20 (운영자 지시): **X 게시 12본/일 → 3본/일 축소**. 슬롯은 07:30(tip·news) / 12:30(앵커 장문) / 20:00(light·engage) 3개. 12본 체제는 소재가 하루 1〜2개 기사에서만 파생돼 기둥 편중·유형 연속이 07-18·07-19 자기 리뷰에서 2회 연속 지적됐고, 생성 부하가 07:00 타임아웃 사고로도 이어졌다. 3본 체제의 방어선은 <strong>본당 밀도 + 주 단위 분산</strong>이다 — 같은 유형 2연속 금지는 하루 안이 아니라 전날 같은 슬롯과의 비교로 적용한다. 변경 지점: daily-x SKILL 믹스표, engine/morning-daily-x.sh(count=3·임계 3·타임아웃 1200s), engine/send-daily-x.sh(슬롯 매핑), com.effloow.x-reminders.plist(3슬롯, 재로드 완료), scripts/loop-tick.mjs(xqa 임계 10→3).

- 2026-07-19 (자기 리뷰 ⑤): **타임아웃 폴백 재생성이 최신 SKILL 제약을 우회한다** — 07:00 X 생성이 2400s 타임아웃(계정 한도 추정)으로 실패 → 07:51 수동 재생성. 결과물은 07-18 정정 2건(유형 2연속 금지·소재 최소 2기사/2기둥)을 <strong>둘 다 통과</strong>(배열 light/tip/light/tip/anchor/tip/engage/tip/light/tip/engage/light — 인접 동일 0건, 소재 4+기둥·4+소스 분산)했으나, 이는 07-18 정정을 코드화한 SKILL 분산 제약이 <strong>아직 반영 안 된 상태</strong>에서 나온 "수동 통과"다. 우연에 의존하면 다음 자동 생성분에서 재발한다. 폴백(수동 재생성) 경로에도 "직전 리뷰 정정 목록"을 명시적 체크리스트로 태울 것 — 회귀 방지는 사람의 감이 아니라 생성 프롬프트에 박아야 한다.

- 2026-07-19 (자기 리뷰 ⑤): **②EM·④서비스빌더 기둥 실종의 반복(회귀)** — 07-18에 이어 오늘도 하루 산출물 전체(블로그 wcag22 a11y 1본 + X 12본)가 ①웹개발(a11y·기술SEO·성능)과 ③AI캐치업(로컬LLM·토큰비용)에만 집중, ②·④ 소재 0건. X는 하루 내 분산(4+기둥)이 개선됐으나 그 "기둥"이 ①③ 안에서만 쪼개진다. 주간 전략 리뷰(1cd1efc)의 "How-to 편중·소재 단일화" 경고와 수렴 — 두 신호가 같은 곳을 가리킨다. 처방: 백로그 도메인 다양화(+6 적재됨)를 X tip 소재 선정에도 강제하고, <strong>주 단위로 ②/④ 소재 최소 각 1본 쿼터</strong>를 캐치업/백로그 단계에서 확보할 것. (블로그 wcag22 자체는 QA·페르소나 DNA 정합 — in-media-res 도입·W3C 공식 인용·9/18 FAIL 실측·"a11y 92점이 AA 실패를 가림"의 함정 서사 — 품질 결함 아님. 문제는 개별 글이 아니라 포트폴리오 편중이다.)

- 2026-07-19 (자기 리뷰 ⑤): **SendMessage 미보고 = 에이전트 유휴로 오인** — 에이전트가 최종 텍스트 응답만 남기면 메인 루프에 미전달되어 무보고 유휴로 보이던 문제를 e944ec1로 공통 헤더 수정(SendMessage 필수). 이번 리뷰가 그 수정의 첫 검증 케이스 — 완료 보고를 반드시 SendMessage로 낸다. 이후 ⑤/⑧ 등 보고형 에이전트의 무보고가 재발하면 프롬프트가 아니라 하니스(도구 노출) 쪽을 의심할 것.

- 2026-07-18 (첫 자기 리뷰 ⑤): **세션 교대·순차 캠페인의 배치 누락** — 문체 캠페인 배치13에서 turboquant 1편이 맺음말 보강에서 빠져 별도 커밋(4e8f942)으로 보충됐다. 매니페스트 done 마킹과 실제 diff가 원자적이지 않다. 교대 인수인계·⑨ 배치 커밋 직후에 `git show <커밋> --stat`으로 needs 대상 편수와 실제 변경 편수를 대조하는 스팟체크를 넣을 것.

- 2026-07-18 (첫 자기 리뷰 ⑤): **X 생성의 소재 편중 → 4기둥 편중** — 07-18 12본이 기사 2건(content-visibility·robots)에서만 파생돼 ① 웹개발 기둥에 쏠리고 ② EM·④ 서비스 빌더가 실종했다. 소재가 같으니 post-2→3 tip 2연속(첫 QA가 지적한 그 건)도 재발했다. **생성 프롬프트에 "하루 소재는 최소 2개 기사/2개 기둥에서 분산 배정, 같은 기사 파생 tip은 연속 배치 금지" 제약을 추가**할 것. 게시 후 QA가 tip 연속을 잡아도 배달은 launchd라 정정이 반영 안 되므로, 순서 결함은 생성 단계에서만 막을 수 있다.

- 2026-07-18 (첫 자기 리뷰 ⑤): **되돌림 결정의 근거 빈약** — AdSense 재도전 revert(b62d388)가 외부 승인 가이드 영상 1건을 근거로 이전 축소 결정(low-value 회피 목적의 공개글 162→35편)을 되돌렸다. 같은 저품질 지적의 재발을 막으려면 재도전은 공개글 수 확대가 아니라 품질 게이트 강화와 병행해야 한다. 되돌림 커밋에는 되돌리는 원래 결정의 근거와 그것이 왜 더는 유효하지 않은지를 본문에 남길 것.

- 2026-07-18 (첫 실전 QA): 생성 단계에서 **유형 배열을 파일 번호에 먼저 할당하고 나서 본문을 쓸 것** — 07-18분에서 post-2→3이 tip 연속으로 배치됨(같은 유형 2연속 금지 위반). 또한 tip 유형도 감정·리액션 어휘 1개+ 규칙 적용 대상임을 생성 프롬프트에서 누락하지 말 것 (post-3·11에서 미달).

- 2026-07-18: 시스템 설계. wbai에서 이식한 검증된 규칙 3건을 초기 교훈으로 등재 — ① 재생성 에이전트는 직전 리뷰 정정 목록을 체크리스트로 재적용(회귀 방지) ② 시효성 소재("오늘/本日" 서술)는 큐 뒤에 넣지 말 것 — 지연되면 거짓이 된다 ③ 수치가 담긴 산출물은 전송 전 원본 데이터와 전건 대조.
