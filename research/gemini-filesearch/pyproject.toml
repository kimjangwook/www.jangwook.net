[project]
name = "gemini-filesearch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=1.49.0",
    "python-dotenv>=1.2.1",
    "tqdm>=4.67.1",
]

[tool.uv.workspace]
members = [
    "demo",
]
